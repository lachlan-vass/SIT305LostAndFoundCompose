<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lachlanvass.sit305lostandfoundcompose">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SIT305LostAndFoundCompose">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".LostAndFoundMapActivity"
            android:exported="false"
            android:label="@string/title_activity_lost_and_found_map"
            android:theme="@style/Theme.SIT305LostAndFoundCompose"

            />
        <activity
            android:name=".SinglePost"
            android:exported="false"
            android:label="@string/title_activity_single_post"
            android:theme="@style/Theme.SIT305LostAndFoundCompose" />
        <activity
            android:name=".CreateActivity"
            android:exported="false"
            android:label="@string/title_activity_create"
            android:theme="@style/Theme.SIT305LostAndFoundCompose" />
        <activity
            android:name=".ListActivity"
            android:exported="false"
            android:label="@string/title_activity_list"
            android:theme="@style/Theme.SIT305LostAndFoundCompose" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SIT305LostAndFoundCompose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>